{% extends "base.html.twig" %}


{% block title %}{{ parent() }} &ndash; Aktie hinzufügen{% endblock %}


{% block body %}
    <div class="container py-3">
        <h1>Aktie hinzufügen</h1>
        <form class="stock-form panel panel-default" action="{{ path("stock_add") }}" method="POST">
            <div class="panel-body">
                {{ form_widget(form) }}
            </div>
            <div class="panel-footer">
                <button type="submit" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Aktie hinzufügen
                </button>
            </div>
        </form>
    </div>
{% endblock %}
